# Remake brief: recursion-01 fresh-session correction pass (VISION outcome C)

Purpose: the VISION-mandated remake — a FRESH actor remakes a study by
applying its saved corrections, without the original session's context
momentum. The maker pane verifies and ledger-rows the result; it does not
co-build.

Status: APPROVED (ShitRat nod for Joel, 2026-09-14 ~2:45am PT) — plan as
proposed, no amendment. Execution order is binding: the remake starts
ONLY after clock-04's cadence ship on 2026-09-15. One bounded actor in a
separate pane, ONE pass, no fan-out, no second child, no peer
orchestration. Hard stop if the child wants a sibling or multi-pass —
come back with an ask first. This brief is the handoff packet.

## Provenance chain (what already happened, in-session)
- recursion-01 @ 972174c shipped (three-layer frame: source-now plane,
  delay-ring memory panel, pointer-canvas intervention with flash).
- critique retained it with an UNRESOLVED composition note: pointer plane
  slightly small / dead-center.
- The maker's OWN IN-SESSION increment (fcc87a2) already scaled/offset the
  pointer plane (3.2×1.2 → 4.0×1.5, offset to (3.6, 1.05, −1.4)) — so the
  fresh remake must NOT merely re-do that scale change; that correction is
  consumed. THIS is the reason a fresh actor is wanted: the remake applies
  the corrections that REMAIN, from a clean reading.

## Corrections remaining for the fresh actor (from critiques/recursion-01.md)
1. Memory layer showing its DELAY visibly: the open question asks whether
   the memory panel can SHOW the (now vs n−24) misalignment, not only
   carry it. The clock lane SOLVED this grammar in clock-02 (same-view
   ring capture, DELAY-visible motion mismatch); the remake may adopt the
   same-view capture for the knot material — cite clock-02 if used.
2. Subtraction pass: with the pointer plane now off-center and larger,
   judge whether its raster decoration still earns its place (the clock
   lane's verdict: such panels fail the subtraction test). Removing it and
   letting the MCP pointer drive a direct intervention on the footage
   plane is an honest direction.
3. Composition: re-frame the three layers so the SOURCE is clearly the
   protagonist (the clock lane's footage-as-protagonist setting is the
   verified template; the knot should hold the same authority).

## Rules for the fresh actor
- One pass, ≤2 repair attempts; failures ledgered stage=failed.
- Full verified ship chain or nothing: turbo check test build →
  archive-ship → upload → 200 → commit explicit paths → ledger row
  (lineage: branches recursion-01-fcc87a2) → ShitRat link.
- Read VISION.md, AGENTS.md, and the two study skills first.
- The remake is judged by the study-critique rubric on RENDERED output,
  with a local-eye (Qwen2.5-VL) pass; verdict may be unresolved.

## Verification protocol (maker pane, after the fresh actor lands a claim)
- Multi-frame browser probe (≥3 frames, ≥90s), console-o11y receipts,
  archive 200 check, readback of the ledger row, then counter-critique.

## Spawn mechanics (settled 2026-09-14, pre-cadence; execution still gated on clock-04 ship)
- Mechanism: `herdr_agent` start with kind=pi in its own workspace pane — matches the
  approved shape (separate pane, fresh context, one bounded actor, no fan-out).
- Handoff packet: THIS FILE + instruction to read VISION.md, AGENTS.md, both study
  skills before touching code.
- Maker-pane role after landing: verification protocol below + ledger row + ShitRat
  link. The maker does NOT co-build; steer only for protocol compliance questions.
- Hard stop triggers: child requests a sibling, a second pass, or edits outside
  apps/sketch + state; kill pane, bring the ask to Joel via ShitRat.
