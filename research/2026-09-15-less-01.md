# Study: less-01 — nothing added (chain before code; pure-subtraction slot)

Direction: art-direction.svx 2026-09-13 "Do less on purpose": one study
that removes something from yesterday's and explains what the removal
revealed. Queue audit 2026-09-14: clock-04 removes two components but
still adds the shard mechanism — it does not satisfy the provocation on
its own. less-01 is the pure-removal slot.

## The chain

**Source finding:** clock-03 shipped with two surviving overlays the
critique called decoration-adjacent (pointer raster panel; wipe band);
clock-02's same-view ring remained the strongest verified temporal
evidence in the lane (motion mismatch at hard cuts, retain verdict).

**Artistic question:** if everything the gallery adds to the film is
taken away except the one verified-lag panel, is the remaining image MORE
legible as expanded cinema, or does it collapse back into television?

**Material:** Conquerb1943 cut, unchanged (provenance clock-01).

**Three.js operation:** delete. Rebuild clock-02's mount with: no wipe
band, no pointer raster panel, no title tag plane — only the footage plane
and the same-view DELAY ring panel. No new geometry, no new uniforms, no
new o11y events beyond the retained heartbeat family.

**Observable result:** the hard-cut motion mismatch (already proven in
clock-02 probes f1–f3) remains visible with strictly less apparatus; a
side-by-side against the clock-02 archive (`/archive/clock-02/076405c/`)
is the acceptance evidence.

**Lineage:** pure-subtraction branch of clock-02 (`clock-02-076405c`,
retained); sibling to clock-04, orthogonal to phone-01/clock-05.
