# Study: clock-03 — the intervention interrupts (planned; chain first)

Move: **Extend** clock-02. Written before code, per expanded-cinema-research.

## The chain

**Source finding:** *Conquer by the Clock* (1943, Prelinger `Conquerb1943`,
CC Public Domain verified on item metadata; provenance
`clock-01-conquerb1943` in research/provenance.md). The film's own device:
title cards and clocks INTERRUPT the workers' day — the apparatus cuts into
lived time. Its hard cuts (verified in clock-01/-02 probes: credits card →
machinery → "U.S. DECLARES WAR" newspaper) are the material's own
interruptions.

**Artistic question:** When the intervention layer (the gallery's live
pointer) acts, should it act ON the footage — wiping, notching, re-timing
the archive itself — rather than sitting beside it as an instrument panel?

**Material:** same clip, `/videos/conquerb1943.mp4` (cut sha
`02b596fb075d7ee35e44c09d14466b27d6d618d7156c0afd26a7331a09cae056`),
CC PD dedication. No new pull needed; lineage carried in place.

**Three.js operation:** keep clock-02's centered footage + visible-lag
memory panel. Change the intervention layer: on every same-study pointer
move, the pointer's sha bytes drive a short WATERFALL WIPE across the
footage plane — a vertical scanline where the past (memory target's texture,
sampled at wipe-position) replaces the now for ~700ms, then decays. The
archive is interrupted by the gallery's own clock, exactly as the film
interrupted its workers.

**Observable result:** within ~10s of a pointer PUT (via the approved MCP
endpoint), a vertical wipe crosses the central footage with visibly wrong
(out-of-time) content inside the wipe band; memory panel keeps lagging.
Checked: 3-frame probe across at least one live pointer move, o11y
`recursion.pointerCut` + new `clock03.wipe` event, zero console errors.

**Lineage:** extends `clock-02-076405c` (footage-as-protagonist, shown-lag
memory); keeps material and framing; changes the intervention layer from an
adjacent panel to an interruption of the footage itself.
