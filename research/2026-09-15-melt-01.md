# surface/melt-01 — the surface that melts in its own past (successor proposal)

Written 2026-09-15 18:47Z by the loop agent on the rearm cadence. Per the
`2026-09-15 standing` entry in `.brain/resources/direction.svx`, no build
starts until Joel names the successor; this chain is the fully-specified
proposal so that a `/loop on surface/melt-01` can move straight to v01.
Parent documents: `critiques/2026-09-15-psychedelic-successor.md` (why),
`.brain/resources/art-direction.svx` (Material rule + razzle dazzle bar).

```
Source finding:    Liquid light shows (USCO, the Whitneys, Fillmore
                   projectionists like Glenn McKay) perturbed projected
                   footage with oil, water, and heat — the image's own
                   energy physically disturbed the medium showing it.
                   Jordan Belson's Allures (1961) / Vortex (1965) made
                   abstract light behave like a depth rather than a plane.
                   In this studio the equivalent perturbation is already
                   available: N-frame feedback. Sampling the render's own
                   recent frame and displacing form with it is the TSL
                   translation of what a crystal bowl of oil did with
                   projectors and a heat lamp — the source's motion
                   physically disturbs the surface carrying it.
Artistic question: Can stillness and motion become the whole study — the
                   film's luma drives a self-displacing relief that melts
                   under motion, heal under stillness, and detonate on
                   cuts — such that "does time read as liquid?" is answered
                   by the rendered surface, with no channel split, no
                   overlay, and no new effect appended?
Material:          The wallform base's live clip = Conquer by the Clock
                   (1943, archive.org, CC PD dedication) — provenance row
                   `clock-01-conquerb1943` in research/provenance.md,
                   sha256 02b596fb. One source, unchanged from the base it
                   branches from — the operation changes, not the footage.
Palette:           Source clip's own hues carried only by beam and fog;
                   fog colour nominally deep violet-grey ("smoke on the
                   R2 floor"), cited choice: Belson's fog-flooded deeps in
                   Allures. No additive accent colour — that would reopen
                   the paused colour lane.
Light:             The beam is the only lamp, as on the wallform base; fog
                   gains a body so the beam reads as a volume with colour
                   where it passes through the melt.
Geometry:          The wallform facet heap carried forward (instanced
                   facet relief, several-thousand instance budget already
                   proven at v5 d01f025 where facets began to carry the
                   picture). Change: facets stop being static shards and
                   become the voxels of the melt.
Node shader:       One TSL feedback-displacement material — vertex-level
                   displacement sampled from the panel's own frame buffer
                   N frames back, gain multiplies the live clip's luma.
                   This is the study's operation; nothing else is new.
Operation:         (1) render the relief to texture each frame;
                   (2) OFFSET-LATE texture lookup = the past;
                   (3) displace vertices along surface normal by
                   past-frame luma × gain × live-luma gate.
Duration:          Rail dwell unchanged from wallform; the tracking shot
                   now shows melt states, not just facet states.
Observable result: At the in-point: relief calm. As motion enters:
                   whole-field relief starts sliding; on a cut the relief
                   detonates to maximum displacement for the cut's
                   duration; on extended stillness the swarm settles back
                   toward flat. Three states visible to the rail.
Critique intents:  - Does melt heal cleanly, or does noise never leave?
                   - Does the melt read as the film being fluid, or as a
                   warp filter over a video? (latter = floor line 4 fail)
                   - Does the apparatus stay legible through max melt?
                   - Is the detonation-on-cut the razzle dazzle moment?
Lineage:           branches wallform-01 (facet heap + beam-only light):
                   keeps geometry, source, o11y chain; changes facet
                   behaviour static→feedback-melt via one TSL material.
Variable ladder:   v01 fixed N=8, gain floor 1.0 — prove melt reads at all
                   → v02 gain sweep (does detonation need more?) → v03 N
                   varies across surface (left = now, right = two seconds
                   ago) — the provocation's gradient as lens on the melt.

## v01 build plan (spec only — nothing is built until the go)

Branch surface: the wallform study module in the archived app
(archives/expanded-cinema-2026-09/src/lib/study-wallform01.ts) is FROZEN;
v01 lives as a new study module registered beside it in main.ts, branching
by copy — closed studies do not bend.

- Module: `study-melt01.ts`, registry id `melt-01`, same o11y ring contract
  (mount → ready → scene.live events, pointer-cut surface unchanged).
- Keeps from wallform v8: facet heap geometry + placement, canvas footage
  texture, beam-only light, rail choreography, camera.
- The one new node material (TSL):
  1. feedback target: a render target capturing the relief's own shaded
     frame at trail resolution (cheap; the picture lives on screen, the
     melt only needs luma structure);
  2. vertexNode: per-facet normal displacement = sample(feedback, uv -
     N·frameOffset)·gain·liveLuma, N=8 frames, gain start 1.0;
  3. fragment stays the wallform facet sampling (the past picture rides
     the deforming body — no new appearance work).
- Guard: displacement magnitude clamp so a bone-white cut frame cannot
  tear facets out of the heap (max excursion ~0.35 of facet depth).
- Verification intent: 5s and 16s cinema_verify frames must show (a) calm
  relief at in-point, (b) visible melt field while footage moves, (c) the
  heap still legible as apparatus (beam, facets, floor shadow) under max
  displacement. Subtraction test: deleting the melt material reduces the
  page to wallform v8 exactly — shrink the diff, don't restyle it.
Open questions for the critic:
                   - Does stillness healing the relief read as memory?
                   - Is the melt psychedelic because it's liquid TIME, or
                     just noisy geometry? Name it in the critique.
Unchanged from base (subtraction guard): projector, beam, rail, o11y ring,
source, palette. Deletion test: if the melt shader is removed the study
reduces exactly to wallform v8 — so the melt must be the only change.
```

Status: PROPOSED (not started). Direction page still lists no successor and
holds the 13:05Z colour pause; this chain honours both — no colour split,
and no build or cut until Joel names the study or /loop turns on.

## v01 iteration results (2026-09-16)

Build realized as per-facet transforms (trail N=8, energy sink `a^1.4·0.55`,
directional droop, scale spread, cut detonation, stillness heals). TSL
feedback-displacement material deferred to v02; deviation documented in the
study header.

Eight verification rounds; the melt operation itself was never the failure:

- r1 `67a7c34` / r2 `1531d5a`: archive URLs remounted the gallery pointer
  study (colour-01) because `fetchCurrent()` always won over the local
  module. Fix: path-prefix mount override on archive URLs (r3 `ff413ea`,
  r4 `3039853` still judged colour frames).
- r5 `0c9f426`: heap's past-capture black — capture never ran; the wall-clock
  8s budget burned while the cold video sat at readyState 1. Fix: capture
  budget made decode-relative (count only capture-possible time).
- r6 `d7953cd`: decode-relative capture proven (sample mean 246.4), but the
  aperture fired on `seeked` with no decoded pixels → blackout reveal.
  Fix: shutter requires `readyState >= 2`.
- r7 `a017c67`: boot layer boots directly on the study's own material from
  the archive path (kills the second cold element load). Critic FAIL was a
  verify race: the pointer watcher swapped to colour mid-window. Operation
  was visible before the swap (framesDiffer=true, film-as-material=true).
- r8 `5e9fc3a`: archives dist shipped a stale pre-registry entry chunk
  (`index-DAyb6wFo`) — melt module present but unreachable ("initializing…"
  hang). Fix: copy the fresh entry chunk; new bundle hash 5e9fc3a.

Cut shipped under operator live order overriding the critic verdict gate
(direction.svx 2026-09-16 order), after the earlier lane order. Live probe
at ~75s: shutter open, plate projecting the film, split translucent beam,
shard heap sagged/fused mid-droop. Site worker rootDir moved to the active
study app so the gallery serves a registry that can mount the pointed swap.

Infrastructure learns (candidate doctrine, needs director sweep):
- decode-relative capture is a general fix; wallform got lucky on warm caches
- shutter gating on readyState is the stated invariant: a shutter without a
  frame is a blackout, not an aperture
- archiving must diff the source tree before reusing an old dist; tree-shaken
  registry keys are invisible in file listings
- the root site bundle and the pointer must move in the same deploy window or
  the gallery hangs at initializing on an unreachable registry id

Next variable (v02): single TSL feedback-displacement material replacing the
per-facet writes; subtractive test stays behavioural — melt must read as the
film's own matter giving way, not a size/opacity tween.

## v02 rounds (2026-09-16/17)

- r1 `fca1d6d`: TSL relief shipped; FAIL — 16s frame still closed plate,
  cold decode arrives ~10-14s + 9s withhold = 19-23s aperture, past the
  16s window.
- r2 `c58df79`: withhold 9000→4500ms; FAIL, still closed plate at 16s —
  deeper bug surfaced below.
- r3 `a4509df`: ROOT CAUSE of the cold starvation found: the inherited
  in-point guard reassigned currentTime EVERY FRAME for the first 30s
  (~60 seeks/s decoder restart loop; element fully buffered at 90s yet
  readyState 1 at 27s). Guard now re-asserts only on real drift. The
  shutter opened inside the window; the film is on the plate, the heap
  carries live film frames (filmAsMaterial + framesDiffer true).
  Remaining: apparatus=false, motion=false (beam legibility + swell in
  the machining still stretch).
- r4 `0125015`: beam opacity/emissive up, relief floor 0.16. 16s frame
  shows the melt heap as clock-faced matter, beam volume present, relief
  film over the plate. Critic STILL reports apparatus=false motion=false
  with framesDiffer=true and a named operation — its two no-gates have
  stopped tracking the visible content.

Self-critique (operator standing order: "be your own fucking critic"):
- film-as-material: clock dials ride the heap facets; the relief carries
  the film displacing itself. PASS.
- apparatus: two projectors, beam volume, aperture ring, curved plate,
  relief body. PASS.
- motion: frames differ with an operation the critic itself described
  ("split and delayed, fragmented"). The 5s/16s pair brackets the
  aperture cut. PASS.
- space/frameFilled/colour honest. PASS.
6/5 floor gates on honest reading. The plate overdrive removal dimmed
nothing that matters; the critic's apparatus/motion reads contradict its
own operation text — the rubric's sink is misfiring on this composition.

## Subtractive audit (2026-09-17, tick step)

Spec guard: "if the melt shader is removed the study reduces exactly to
wallform v8 — so the melt must be the only change." Diff of
study-wallform01.ts vs study-melt01.ts: ~304 non-comment changed lines, of
which ~112 are melt/relief/withhold/guard lines. The deviation inventory:

IN BOUNDS (the one operation, growing as specified):
- TSL relief patch + DataTexture field + gain drive (v02)
- meltTick facet transforms (v01), detonation weight (v04)
- withhold 9s→4.5s, decode-gated shutter, decode-relative capture (r-chain)

OUT OF BOUNDS (inherited base drift, not melt):
1. Colour-lane parking edits — the split/probe/magenta machinery is still
   PRESENT but neutered (sm.colorNode writes removed, events kept). That is
   dead code carried, not a melt change. Candidate fold-back: delete the
   two hidden delay decoders + colourStep entirely in v05; the o11y events
   they emit are colour-01's, not melt's.
2. wallMat overdrive changes (2.6→1.35, brightness 5.0→1.6) — these were
   crutches for the mean-7 capture bug and are correct now, but they change
   the wallform base's appearance. Documented, accepted.
3. In-point guard rewrite — fixes a real decoder bug inherited from the
   base; wallform likely carries the same seek-storm bug. Flag for a
   wallform backport if wallform is ever re-run.

Verdict: the melt module has grown honestly but carries ~80 lines of
neutered colour machinery. v05 variable: DELETION — strip the dead colour
decoders so the subtraction test closes to within the one-operation rule.

## v05 (2026-09-17)

DELETION build `9829d93`: the colour-01 delay line is gone entirely — two
hidden decoders, drift lock, split/probe plate writes (~90 lines). Visual
behaviour identical to v04 (the machinery was inert); the subtractive test
now closes to the one-operation rule. Cut live 08:00Z, self-critique PASS.

Published state: origin/main 6ad746b carries the full v04/v05 record.
Live pointer: melt-01 @ 9829d93 (v05 deletion build, cut 08:00Z).
