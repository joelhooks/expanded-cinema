# withhold-01 — the image withholds itself

Written 2026-09-14 late DAY 2, per `critiques/clock-04-v21-thread.md`
(seq-22: next chain starts from the shipped critique) and the queued
provocation on `.brain/resources/art-direction.svx` (dream monitor,
2026-09-14 17:40Z). The brief's own sending condition is now met: the
director's own frames verified the reveal chain on v18 (seq-31 confirmed
`clock04.screen.reveal via seeked` on the AD's load; seq-32/33 confirmed the
one-shot work and the Range substrate fix; five cold-load mounts banked
2026-09-14 during the seq-28 audit).

```
Source finding:    The readiness boundary itself became the material of
                   the v13→v18 clock-04 thread. The `seeked` event is the
                   one moment where a media element says "the image is
                   addressable" — and before this chain could even rely on
                   it, the substrate had to grow R2 Range support (206) so
                   the element could seek at all. An aperture that waits
                   for `seeked` is therefore not an artificial gate; it is
                   the medium's own arming edge, discovered by accident and
                   now verified deterministic (one-shot guard, five mounts).
Artistic question: Can the opening of an image — the boundary between
                   withheld and shown — be the whole study: a surface that
                   starts as an authored, unlit absence (a closed aperture,
                   not a loading state), reads its own `seeked` event as the
                   shutter, and lets the DELAY-late beam carry that same
                   arrival across the room a few seconds later?
Material:          videos/conquerb1943.mp4 (provenance: catalog entry;
                   duration 90.0s; in-point 63s verified bright). One
                   source. The same material as clock-01..04, deliberately —
                   the withheld framing is the change, not the footage.
Three.js operation:
                   - screen material starts as an authored CLOSED STATE: a
                     matte near-black plate with a physical iris ring
                     (thin torus edge, unlit) — reads as an apparatus that
                     has not opened, never as a spinner.
                   - the one-shot `seeked` reveal (the v21 guard pattern,
                     proven) is the aperture event: the iris ring lights
                     from the projector side, the plate becomes the film
                     plane in the same frame — a cut, not a fade.
                   - the delayed beam (RING=181, DELAY=180) crosses the
                     room and lands on the back wall; its first illuminated
                     band arrives DELAY ms after the aperture — the past
                     carries the news of the opening into the space.
                   - rail: three authored positions in 48s, each dwelled
                     long enough (≥8s) to catch a different relation —
                     (a) aperture closed, beam dark; (b) aperture just
                     opened, beam still dark (image exists before its
                     past arrives); (c) beam lit, opening now historic.
Observable result: From a cold load: 0–~9s a closed dark plate with an
                   unlit ring, no card, no spinner; at the seeked frame the
                   plate昨cut to the film (in-page o11y event
                   `withhold01.aperture {at}`); ~3s later the wall band
                   lights. Between second 5 and 60 the three rail relations
                   are visibly distinct. Check: five cold loads, 6s and
                   16s frames each, worst attached; the boundary must be
                   deterministic across all five.
Lineage:           REVISITS clock-04 with the readiness boundary as the
                   sole operation — deletes the luma-sampled cone detail
                   (the v21 lesson: geometry debt beat effect work), keeps
                   the delay ring, the two-projector apparatus, and the
                   rail. One operation; the beam's stripes stay a plain
                   band mean (no per-slice luma) — the arrival, not the
                   picture, is the content.
```

No glow, no new slats, no overlay (brief's own constraints). The guard:
the withheld state must read as a closed aperture, not as loading, and the
boundary must be deterministic across five loads before it counts as
material.
