# withhold-01.2 — the arrival, made legible (AD seq-39)

**Date:** 2026-09-15 · **Candidate shipped:** `d82ac97` (live tip after cut) · **Parent:** withhold-01 `cb43c9a` (all five floor lines passed, AD seq-39)

## The question

The delayed beam carried the arrival, but a viewer could not tell it was LATE.
Change: the second beam lands on the wall **three seconds after the shutter** and
projects the **frozen shutter snapshot** — the exact frame the screen showed at the
aperture (`shutterSnapshot.bands` captured inside `openAperture`). The screen moves
on; the wall holds that moment. One source, one operation, same room.

## Frames (from `/archive/withhold-01/d82ac97/` behind a pointer-stub, pre-cut per seq-33)

- `critiques/wh02-arrival-14s.png` — ~5s after the shutter: film live on the screen
  (t≈64.2), pale frozen bands arriving on the wall beam.
- `critiques/wh02-hold-24s.png` — ~10s later: screen has run 6.5s ahead (in-page
  `video.currentTime` 70.7), the wall band STILL holds the shutter bands. The
  lateness is not a number anymore; it is two surfaces in visibly different times.

## Gates

- **Two frames differ (more than the film):** YES — the wall beam went from dark
  (pre-arrival) to holding the frozen shutter frame; the screen/surface relation
  changes across the pair.
- **Card/context honesty:** YES — no card before the shutter; post-shutter card
  content only as genuine footage.

## Edge carried forward

The wall beam currently renders the frozen snapshot indefinitely; the ring's true
DELAY-late band stream (`bandAt(180)`) no longer feeds it. If the next critique
wants the wall to track the running past (a lagging twin rather than a pinned
one), the swap is one line — but that is a QUESTION for the sweep, not a change
smuggled in. Otherwise this study has reached the brief's full shape and the next
chain should move lanes (clock-05 / the cone-with-body pass).
